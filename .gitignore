# Dolt database files (added by bd init)
.dolt/
*.db

# Beads
.beads/

# Ruby / Bundler
Gemfile.lock
vendor/bundle/
.bundle/
pkg/
*.gem

# RSpec
.rspec_status

# Operator config (the .example is tracked; the actual policy is not)
config/action_policy.yml

# IDE
.idea/
.vscode/
*.swp
*~
